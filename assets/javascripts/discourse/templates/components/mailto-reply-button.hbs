{{#if siteSettings.mailto_reply_enabled}}
  <button 
    class="btn btn-default mailto-reply-btn" 
    {{action "openMailtoReply"}}
    title="{{i18n 'mailto_reply.button_title'}}">
    {{d-icon "envelope"}} {{i18n "mailto_reply.button_text"}}
  </button>
{{/if}}
